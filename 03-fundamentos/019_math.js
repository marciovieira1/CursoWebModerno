newFunction()
function newFunction() {
    const raio = 5
    var area = Math.PI * Math.pow(raio, 2) //Função Math com atributos PI e pow(potenciação)
    console.log(area)
    console.log(Math.PI)
    console.log(Math.pow(raio, 2))
}

